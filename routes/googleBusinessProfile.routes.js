const express=require('express');const auth=require('../middleware/auth.middleware');const c=require('../controllers/googleBusinessProfile.controller');const router=express.Router();
router.get('/projects/:projectId/gbp/connect',auth,c.connect);router.get('/projects/:projectId/gbp/status',auth,c.status);router.get('/projects/:projectId/gbp/accounts',auth,c.accounts);router.get('/projects/:projectId/gbp/locations',auth,c.locations);router.post('/projects/:projectId/gbp/location',auth,c.select);router.get('/projects/:projectId/gbp/report',auth,c.report);router.post('/projects/:projectId/gbp/sync',auth,c.sync);router.get('/gbp/callback',c.callback);module.exports=router;
