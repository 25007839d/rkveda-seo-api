const db=require('../config/database');
const PLATFORMS=['facebook','instagram','linkedin','youtube','x','tiktok','other'];
async function ensure(){await db.query(`CREATE TABLE IF NOT EXISTS social_profiles (id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,project_id BIGINT UNSIGNED NOT NULL,platform ENUM('facebook','instagram','linkedin','youtube','x','tiktok','other') NOT NULL,handle VARCHAR(255) NULL,profile_url VARCHAR(1000) NULL,followers BIGINT UNSIGNED DEFAULT 0,following BIGINT UNSIGNED DEFAULT 0,posts_count BIGINT UNSIGNED DEFAULT 0,verified TINYINT(1) DEFAULT 0,notes TEXT NULL,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,UNIQUE KEY uq_social_profile(project_id,platform),CONSTRAINT fk_social_profile_project FOREIGN KEY(project_id) REFERENCES seo_projects(id) ON DELETE CASCADE) ENGINE=InnoDB`);await db.query(`CREATE TABLE IF NOT EXISTS social_metrics_daily (id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,project_id BIGINT UNSIGNED NOT NULL,platform ENUM('facebook','instagram','linkedin','youtube','x','tiktok','other') NOT NULL,metric_date DATE NOT NULL,followers BIGINT DEFAULT 0,posts BIGINT DEFAULT 0,reach BIGINT DEFAULT 0,impressions BIGINT DEFAULT 0,likes BIGINT DEFAULT 0,comments BIGINT DEFAULT 0,shares BIGINT DEFAULT 0,video_views BIGINT DEFAULT 0,clicks BIGINT DEFAULT 0,engagement_rate DECIMAL(10,4) DEFAULT 0,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,UNIQUE KEY uq_social_metric(project_id,platform,metric_date),CONSTRAINT fk_social_metric_project FOREIGN KEY(project_id) REFERENCES seo_projects(id) ON DELETE CASCADE) ENGINE=InnoDB`);await db.query(`CREATE TABLE IF NOT EXISTS social_posts (id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,project_id BIGINT UNSIGNED NOT NULL,platform ENUM('facebook','instagram','linkedin','youtube','x','tiktok','other') NOT NULL,post_url VARCHAR(1000) NULL,published_at DATETIME NULL,caption TEXT NULL,likes BIGINT DEFAULT 0,comments BIGINT DEFAULT 0,shares BIGINT DEFAULT 0,views BIGINT DEFAULT 0,clicks BIGINT DEFAULT 0,engagement_rate DECIMAL(10,4) DEFAULT 0,source VARCHAR(50) DEFAULT 'manual',created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,CONSTRAINT fk_social_post_project FOREIGN KEY(project_id) REFERENCES seo_projects(id) ON DELETE CASCADE,KEY idx_social_posts_project_platform(project_id,platform)) ENGINE=InnoDB`);}
async function owned(pid,uid){const [r]=await db.execute('SELECT id,project_name,website_url,domain FROM seo_projects WHERE id=? AND user_id=? LIMIT 1',[pid,uid]);return r[0]||null;}
function badPlatform(p){return !PLATFORMS.includes(p)}
async function overview(req,res){try{const pid=Number(req.params.projectId);if(!await owned(pid,req.user.userId))return res.status(404).json({success:false,message:'Project not found'});await ensure();const [profiles]=await db.execute('SELECT * FROM social_profiles WHERE project_id=? ORDER BY platform',[pid]);const [metrics]=await db.execute(`SELECT platform,SUM(posts) posts,SUM(reach) reach,SUM(impressions) impressions,SUM(likes) likes,SUM(comments) comments,SUM(shares) shares,SUM(video_views) video_views,SUM(clicks) clicks,AVG(engagement_rate) engagement_rate,MAX(followers) followers FROM social_metrics_daily WHERE project_id=? AND metric_date>=DATE_SUB(CURDATE(),INTERVAL 30 DAY) GROUP BY platform ORDER BY platform`,[pid]);const [posts]=await db.execute('SELECT id,platform,post_url,published_at,caption,likes,comments,shares,views,clicks,engagement_rate,source FROM social_posts WHERE project_id=? ORDER BY COALESCE(published_at,created_at) DESC LIMIT 50',[pid]);const [connections]=await db.execute('SELECT platform,account_id,account_name,status,last_synced_at FROM social_connections WHERE project_id=? ORDER BY platform',[pid]);const totals=metrics.reduce((a,x)=>{for(const k of ['posts','reach','impressions','likes','comments','shares','video_views','clicks'])a[k]+=Number(x[k]||0);a.engagement_rate+=Number(x.engagement_rate||0);return a},{posts:0,reach:0,impressions:0,likes:0,comments:0,shares:0,video_views:0,clicks:0,engagement_rate:0});totals.engagement_rate=metrics.length?totals.engagement_rate/metrics.length:0;res.json({success:true,project:await owned(pid,req.user.userId),platforms:PLATFORMS,profiles,metrics,posts,connections,totals});}catch(e){console.error(e);res.status(500).json({success:false,message:'Unable to load social intelligence'});}}
async function saveProfile(req,res){try{const pid=Number(req.params.projectId);if(!await owned(pid,req.user.userId))return res.status(404).json({success:false,message:'Project not found'});await ensure();const p=req.body.platform;if(badPlatform(p))return res.status(400).json({success:false,message:'Invalid platform'});await db.execute(`INSERT INTO social_profiles(project_id,platform,handle,profile_url,followers,following,posts_count,verified,notes) VALUES(?,?,?,?,?,?,?,?,?) ON DUPLICATE KEY UPDATE handle=VALUES(handle),profile_url=VALUES(profile_url),followers=VALUES(followers),following=VALUES(following),posts_count=VALUES(posts_count),verified=VALUES(verified),notes=VALUES(notes)`,[pid,p,req.body.handle||null,req.body.profile_url||null,Number(req.body.followers||0),Number(req.body.following||0),Number(req.body.posts_count||0),req.body.verified?1:0,req.body.notes||null]);res.json({success:true});}catch(e){res.status(500).json({success:false,message:e.message});}}
async function saveMetric(req,res){try{const pid=Number(req.params.projectId);if(!await owned(pid,req.user.userId))return res.status(404).json({success:false,message:'Project not found'});await ensure();const p=req.body.platform,d=req.body.metric_date;if(badPlatform(p)||!d)return res.status(400).json({success:false,message:'platform and metric_date are required'});await db.execute(`INSERT INTO social_metrics_daily(project_id,platform,metric_date,followers,posts,reach,impressions,likes,comments,shares,video_views,clicks,engagement_rate) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?) ON DUPLICATE KEY UPDATE followers=VALUES(followers),posts=VALUES(posts),reach=VALUES(reach),impressions=VALUES(impressions),likes=VALUES(likes),comments=VALUES(comments),shares=VALUES(shares),video_views=VALUES(video_views),clicks=VALUES(clicks),engagement_rate=VALUES(engagement_rate)`,[pid,p,d,...['followers','posts','reach','impressions','likes','comments','shares','video_views','clicks'].map(k=>Number(req.body[k]||0)),Number(req.body.engagement_rate||0)]);res.json({success:true});}catch(e){res.status(500).json({success:false,message:e.message});}}
async function savePost(req,res){try{const pid=Number(req.params.projectId);if(!await owned(pid,req.user.userId))return res.status(404).json({success:false,message:'Project not found'});await ensure();const p=req.body.platform;if(badPlatform(p))return res.status(400).json({success:false,message:'Invalid platform'});await db.execute(`INSERT INTO social_posts(project_id,platform,post_url,published_at,caption,likes,comments,shares,views,clicks,engagement_rate,source) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`,[pid,p,req.body.post_url||null,req.body.published_at?new Date(req.body.published_at):null,req.body.caption||null,Number(req.body.likes||0),Number(req.body.comments||0),Number(req.body.shares||0),Number(req.body.views||0),Number(req.body.clicks||0),Number(req.body.engagement_rate||0),req.body.source||'manual']);res.status(201).json({success:true});}catch(e){res.status(500).json({success:false,message:e.message});}}
async function removePost(req,res){try{const pid=Number(req.params.projectId);if(!await owned(pid,req.user.userId))return res.status(404).json({success:false,message:'Project not found'});await ensure();await db.execute('DELETE FROM social_posts WHERE id=? AND project_id=?',[req.params.id,pid]);res.json({success:true});}catch(e){res.status(500).json({success:false,message:e.message});}}
module.exports={overview,saveProfile,saveMetric,savePost,removePost};
